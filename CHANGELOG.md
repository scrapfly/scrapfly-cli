# Changelog

## Unreleased

- Initial release: `scrape`, `screenshot`, `extract`, `crawl`, `account`.
- JSON envelope output by default; `--pretty` for human summaries.
- Auth via `SCRAPFLY_API_KEY` env or `--api-key` flag.
